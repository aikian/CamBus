# CHANGELOG

모든 주요 변경사항은 이 파일에 기록합니다.

버전 규칙은 Semantic Versioning(`MAJOR.MINOR.PATCH`)을 사용합니다.

## [1.3.2] - 2026-08-30

### Added

- 정류장 승하차 정차시간 `BOARDING_SECONDS`(15초) — 기점(박물관·노천강당)과 서문(5분 정차)을
  제외한 전 정류장에 적용. 1노선 총 19.9분, 2노선 21.7분(주행+정차)으로 ETA·길찾기에 반영

### Changed

- 1노선 음악관 정차 위치를 통과 도로(OSM way 271707498) 위로 옮겨, 북쪽 막다른 지선
  (way 970320813)으로 들어갔다 되돌아 나오던 갈고리 구간 제거
- 1노선 천마관 정차 위치를 도로를 따라 약 21m 남쪽으로 이동
- 1노선 사범대를 중도로 통합 (정류장 9개) — 사범대 좌표는 경유점으로 남겨 실제 주행 경로 보존,
  `사범대`/`사범대학` 검색어는 중도로 연결

### Notes

- 중도–음악관 사이 두 갈래는 **일방통행 쌍**입니다. 남쪽 도로(way 210935470)가 `oneway=yes`
  북향이라 상행만 가능하고, 하행은 북쪽 도로를 쓸 수밖에 없어 한 갈래로 합칠 수 없습니다

## [1.3.1] - 2026-08-30

### Fixed

- 예상 버스 마커가 노선에서 벗어나 보이던 문제 (원인 3가지)
  1. 겹침 구간에서 선만 좌우로 벌어지고 버스는 원래 중심선 좌표를 써서 어긋남
  2. 버스 위치를 시간 비율로 계산해 거리 기준으로 그려진 선에 대입 (정차 중에는 정류장 원좌표로 튐)
  3. 5초 간격 표본 사이를 **직선 보간**해서, 배속을 올리면 한 번에 크게 건너뛰며 건물·코너를 가로지름
  → 위치를 노선 전체 거리 대비 진행 비율로 구하고, `renderRoute()`가 실제로 그린 좌표 위에서
  매 프레임(`requestAnimationFrame`) 다시 계산하도록 변경. 구조적으로 선을 벗어날 수 없음
  (측정: 30배속 실시간 14프레임 최대 이탈 0.85px, 4개 축척 정적 스윕 1,608표본 최대 0.98px, 이전 21.1px)
- 폴리라인 누적거리 표를 `WeakMap`에 캐시하고 이분탐색으로 조회 — 매 프레임 재계산 비용 제거
- 확대·축소나 노선 토글 직후 버스 위치가 한 틱 늦게 갱신되던 문제
- 정류장 좌표를 나중에 편집기로 옮겨도 환승 거리 계산이 옛 좌표를 쓰던 문제

### Changed

- 1노선 빨강(#e63946) / 2노선 파랑(#3478f6) / 환승 보라로 색상 통일 (지도·범례·배지·편집기)
- 예상 버스와 공유 위치 마커를 노선 색 원형 점(흰 테두리, 안에 호선 번호)으로 변경
- 정류장 마커를 번호 대신 짧은 역 이름 알약으로 변경, 좌표가 겹치는 환승역은 마커 하나로 통합
- 순환 종점(기점 복귀 지점)은 시작 정류장과 같은 지점이므로 지도에 중복 표시하지 않음
- 정류장 이름 정리: 중앙도서관→중도, 과학도서관→이도, 1과학관→과학관
- 2노선 거울못(F22/G01)·이도(F27/G07) 왕복 중복 정차점을 각각 하나로 통합
  (도로 경유점은 이어붙여 보존, OSRM 재계산 없음)
- 보행 라우팅 서버 응답이 없을 때 직선거리 기준 추정치로 폴백해 예상 시간이 항상 표시됨
- 로컬(`localhost`/`127.0.0.1`) 접속은 파라미터 없이도 테스트 시간 가속이 켜짐 (`?test=0`으로 해제)

## [1.3.0] - 2026-08-29

### Added

- 길찾기 출발 시간 지정(`지금 출발` / `시간 지정`) — 고른 시각 기준으로 배차·대기·환승을 다시 계산
- 노선 간 자동 환승 인식과 환승 경로 후보 — 학교 공지에서 같은 코드(서문/F21/B04)를 쓰는 정류장을 자동으로 하나의 환승역으로 묶고, 1노선↔2노선 환승 조합을 길찾기 결과에 포함
- 정류장 팝업의 환승 배지(🔄) — 같은 지점/도보 환승 여부와 노선을 바로 표시
- 정류장 이름을 지하철식 2~4글자 단축 이름으로 정리 (예: 서문, 중앙도서관, 1과학관, 사범대)
- 1노선 음악관·2노선 기계관의 출발/복귀 중복 정차점을 하나로 통합 — 저장된 도로 경유점은 손실 없이 이어붙여 재사용
- `?test=1&simTime=HH:MM&simSpeed=N` 테스트 전용 시간 가속 모드 — 실제 시각과 무관하게 지정한 시각부터 N배속으로 흘러 배차/버스 마커/정류장 ETA를 항상 볼 수 있음. 실제 서비스 화면에는 사용하지 않음
- `route-utils.js`에 `buildTransferStations`/`transferWalkSeconds` 순수 함수와 테스트 추가

### Changed

- 학생지원팀 공지(2026학년도 2학기 순환버스 증편) 원문을 다시 대조해 배차표·정류장 수·서문 5분 정차가 정확히 일치함을 확인
- 정류장 편집기/노선 데이터가 새 정류장 수(1노선 10개, 2노선 11개)에 맞춰 재계산됨

## [1.2.0] - 2026-08-29

### Added

- 정류장 사이 수동 경유점과 실측 초를 함께 관리하는 로컬 `path-editor.html`
- 미리 계산해 저장한 `data/route-paths.json` 고정 노선과 재생성 스크립트
- 순환 끝 지점을 넘어가는 승차·하차 경로 계산 유틸리티와 회귀 테스트
- 서버 환경변수 기반 실제 Ezoic 공개 설정 API와 `ads.txt` 리디렉션
- Picks 콘텐츠 `startsAt`/`endsAt` 만료 관리와 링크 검사 스크립트
- Node 내장 테스트 러너 기반 데이터/API 자동 테스트
- 키보드 탭 전환, 포커스 표시, 모션 감소, 모바일 터치 영역 보강

### Changed

- 저장된 고정 노선을 우선 사용하고 외부 OSRM은 경로 데이터가 없을 때만 폴백
- 광고를 유지하면서 모바일 페이지 탭과 길찾기 버튼이 광고에 가리지 않도록 레이어/간격 조정
- Service Worker의 오프라인 폴백과 `127.0.0.1` 개발 등록 지원 개선
- 보안 응답 헤더와 외부 경로 요청 시간 제한 추가

### Operations

- 실제 광고 승인·CMP·placement·검수 절차를 `ADS_PLAN.md`에 분리
- `npm run check`, `npm test`, `npm run check:links`, `npm run refresh:paths` 운영 명령 추가

## [1.1.0] - 2026-08-29

### Added

- 노선 위 시간표 예상 버스 마커 부드러운 이동 애니메이션
- 버스 마커 다음 정류장 ETA 표시
- 정류장 팝업 다음 2개 버스 ETA
- 구간별 실측 ETA 오버라이드 파일 `data/route-timings.json`
- 탑승객 공유 위치 마커 ETA/이동 애니메이션
- Picks 자동수집 데이터 `data/portal-auto.json` 및 수집 스크립트
- 기간형 지역 직접광고 데이터 `data/local-ads.json`
- 길찾기/안내/탑승/Picks 클릭 집계 `data/metrics.json`
- PM 존 전용 데이터 `data/pm-zones.json`
- 자동수집 소스 설정 `data/feed-sources.json`
- `TEST_REPORT.md` 검수 기록

### Changed

- 실제 실측 구간시간이 없으면 기존 OSRM 구간시간으로 자동 폴백
- 크라우드 탑승자 위치가 존재하는 운행편은 시간표 예상 마커보다 우선 표시
- Service Worker 캐시 대상에 v1.1.0 데이터 파일 추가

### Known limitations

- 실제 차량 GPS는 제공되지 않음
- `route-timings.json`의 실측 구간시간은 아직 미입력 상태이며 OSRM 폴백 사용
- 자동수집 스크립트는 대상 학교 페이지 구조/네트워크 상태에 따라 실패할 수 있으며 수동 피드는 보존됨
- 컨테이너의 Chromium 네트워크 대기 문제로 전체 외부 지도 타일/라우팅 E2E는 제한적으로 검증됨

## [1.0.0] - 2026-08-29

### Added

- CamBus / Picks 좌우 2페이지 구조
- OpenStreetMap 기반 영남대 캠퍼스 지도
- 1노선/2노선 정류장 및 노선 표시
- 2026학년도 2학기 배차표 반영
- 시간표 기반 버스 예상 위치와 ETA
- 도보 / 도보+순환버스 경로 비교
- 승차/하차 Guidance 및 브라우저 알림 구조
- 탑승자 동의형 익명 위치 공유
- 사용자 신고형 혼잡도
- PM/전동킥보드 주차 레이어
- 세션 최초 대형 광고 슬롯
- 메인 하단 얇은 광고 슬롯
- Picks 세로형 배너/카드 피드
- Picks 콘텐츠 5개당 광고 1개 자동 삽입
- `ad@uxo.kr` 지역 광고 모집 하우스 배너
- Picks 최하단 오늘/이번달 사용자 수
- Picks 사용자 통계 아래 별도 광고 배너
- `data/portal-feed.json` 파일 기반 콘텐츠 운영
- 초기 공개 데이터 20개 수집/시드
- Ezoic launch / bottom / feed placement 연결 어댑터
- PWA Manifest 및 Service Worker
- 버전 파일 `VERSION`
- SRS / README / DATA_SOURCES 문서화

### Changed

- 우측 페이지를 기존 4열 앱 아이콘 방식에서 세로형 생활정보 피드로 전환
- 유용한 사이트 데이터 소스를 `data/useful-sites.json`에서 `data/portal-feed.json`으로 변경
- API를 `/api/useful-sites`에서 `/api/portal-feed`로 변경

### Notes

- 실제 차량 GPS는 연결되어 있지 않음
- 시간표 ETA는 실제 GPS와 구분하여 표시
- 식당/카페 데이터는 공개 지역 검색을 바탕으로 한 초기 추천이며 운영 중 주기적 검수 필요
