# CamBus — Software Requirements Specification

- 문서 버전: **3.0.0**
- 제품 릴리스: **1.1.0**
- 작성일: **2026-08-29**
- 대상: 영남대학교 경산캠퍼스 교내 이동 웹앱/PWA
- 제품명: **CamBus**
- 서브 페이지명: **Picks**
- 기준 지도: OpenStreetMap + Leaflet
- 기준 버스 데이터: 2026학년도 2학기 순환버스 시간표 및 정류장 자료

---

## 1. 목적

CamBus는 영남대학교 경산캠퍼스에서 학생·교직원이 다음 작업을 빠르게 수행하도록 하는 모바일 우선 웹앱이다.

1. 현재 위치에서 목적지까지 도보와 순환버스 결합 경로를 비교한다.
2. 실제 차량 GPS가 없어도 시간표 기반 ETA를 제공한다.
3. 사용자가 명시적으로 동의하면 탑승 중 익명 위치를 공유해 버스 위치 추정을 보정한다.
4. 사용자 신고 기반 혼잡도를 제공한다.
5. 승차·하차 알림을 제공한다.
6. 확인된 PM/전동킥보드 주차 정보를 지도에 표시한다.
7. 우측 Picks 페이지에서 학교소식·유용한 링크·맛집·지역정보를 세로형 피드로 제공한다.
8. 광고를 길찾기 UX와 충돌하지 않게 배치하면서 서비스 수익화를 지원한다.

---

## 2. 제품 범위

### 2.1 포함

- OpenStreetMap 캠퍼스 지도
- 1노선(인문계), 2노선(자연계)
- 정류장 마커 및 설명
- 배차 시간표
- 시간표 기반 버스 예상 위치/ETA
- 현재 위치 기반 도보 경로
- 도보 + 순환버스 경로 비교
- 승차/하차 Guidance
- PWA
- 동의형 크라우드 위치 공유
- 사용자 신고형 혼잡도
- PM 주차 존 레이어
- 좌우 2페이지 UI
- Picks 세로형 배너 피드
- 피드 콘텐츠 5개당 광고 1개 자동 삽입
- 지역 광고 모집 하우스 배너
- 오늘/이번달 익명 사용자 수
- Ezoic 연결용 광고 어댑터
- 파일 기반 운영 데이터 관리

### 2.2 제외

데이터/제휴가 없는 상태에서는 다음을 제공하지 않는다.

- 학교/운수사 실제 버스 GPS
- 통신사 기지국 데이터 기반 버스별 승객 수
- 공유 킥보드 업체 실시간 기기 위치/잔여 대수
- 결제/대여/반납 기능
- 교통카드 연동
- 네이티브 OS가 보장하는 완전한 백그라운드 위치 추적
- 관리자 웹페이지

---

## 3. 용어

| 용어 | 정의 |
|---|---|
| ETA | Estimated Time of Arrival, 예상 도착 시각/남은 시간 |
| 시간표 ETA | 배차시각 + 예상 구간 운행시간 + 정차시간 기반 ETA |
| Crowd 위치 | 탑승자가 동의하여 공유한 휴대폰 GPS를 운행편 단위로 집계한 위치 |
| PM | Personal Mobility, 전동킥보드 등 개인형 이동장치 |
| Guidance | 승차/대기/탑승/하차 단계별 안내 모드 |
| Picks | 우측 생활정보/콘텐츠 피드 페이지 |
| 하우스 광고 | 외부 광고 네트워크가 아닌 CamBus 자체 광고 모집/홍보 배너 |

---

## 4. 사용자

### 4.1 일반 사용자

- 지도 조회
- 현재 위치 확인
- 목적지 검색
- 경로 비교
- ETA 확인
- 승하차 안내
- Picks 콘텐츠 조회

### 4.2 자발적 기여 사용자

- 탑승 후 익명 위치 공유 동의
- 버스 혼잡도 신고

### 4.3 운영자

관리자 UI 없이 파일을 수정한다.

- `data/portal-feed.json`: Picks 콘텐츠
- `app.js`: 정류장/노선/PM 존 데이터
- `index.html`: 광고 placement 설정
- `DATA_SOURCES.md`: 데이터 출처 검증 기록

---

# 5. 기능 요구사항

## FR-001 지도

- OpenStreetMap 타일을 표시한다.
- 캠퍼스 중심으로 초기화한다.
- 확대/축소/현재 위치/전체보기 기능을 제공한다.
- OSM attribution을 유지한다.

## FR-002 노선 및 정류장

- 1노선과 2노선을 서로 다른 색으로 표시한다.
- 정류장 번호를 마커에 표시한다.
- 팝업에 노선, 번호, 정류장명, 건물코드, 정차 설명, 주요 이용처를 표시한다.
- 동일 장소를 두 노선이 사용하는 경우 공용 정차임을 시각적으로 구분한다.

## FR-003 배차표

2026학년도 2학기 기준:

- 08:00~10:50: 10분 간격
- 11:00 / 11:20 / 11:40
- 점심시간: 운행 없음
- 13:00~17:40: 20분 간격
- 서문 순환지점: 5분 정차

신뢰 가능한 운행 요일/휴일 조건이 없는 경우 임의 생성하지 않는다.

## FR-004 시간표 기반 ETA

실제 버스 GPS가 없을 때 다음 입력을 사용한다.

1. 기준 배차 시각
2. OSM/OSRM 도로 경로
3. 구간 예상 주행시간
4. 서문 5분 정차
5. 정류장 순서

화면에는 `예상`, `시간표 기준`, `실제 GPS 아님` 중 하나 이상을 표시한다.

## FR-005 목적지 및 경로 탐색

최소 다음 후보를 계산한다.

1. 직접 도보
2. 도보 → 1노선 → 도보
3. 도보 → 2노선 → 도보

각 후보는 다음 정보를 가진다.

- 총 예상 소요시간
- 도보 거리
- 승차 정류장
- 예상 대기시간/승차시각
- 하차 정류장
- 버스 탑승시간
- 단계별 안내

가장 빠른 후보를 우선 표시한다.

## FR-006 승하차 Guidance

버스 경로에 `승·하차 알림 시작`을 제공한다.

상태:

- `to_board`: 승차 정류장으로 이동
- `waiting`: 정류장 도착 및 버스 대기
- `riding`: 탑승 중
- `completed`: 하차 완료

기본 임계값:

- 승차장 접근: 약 220m
- 승차장 도착: 약 70m
- 버스 도착 임박: ETA 3분
- 하차장 접근: 약 350m
- 하차 직전: 약 120m

지원 가능한 경우 다음 알림 채널을 사용한다.

- 화면 안내 바
- Browser Notification
- 진동
- 알림음

## FR-007 크라우드 위치 공유

- 기본값은 OFF다.
- 사용자가 탑승 후 별도 동의한 경우에만 전송한다.
- 이름, 학번, 전화번호를 수집하지 않는다.
- 서버는 약 2분 TTL로 인메모리에 유지한다.
- 외부 API는 개별 사용자 위치 목록이 아니라 운행편별 집계 위치만 반환한다.
- 참여 표본이 없으면 시간표 ETA로 폴백한다.

## FR-008 혼잡도

사용자는 다음 상태 중 하나를 신고할 수 있다.

- 여유
- 보통
- 혼잡
- 만석

요구사항:

- 사용자 신고 데이터임을 명시한다.
- 실제 전체 승객 수처럼 표현하지 않는다.
- 일정 TTL 후 자동 만료한다.

## FR-009 PM/킥보드 레이어

- 지도 레이어 토글을 제공한다.
- 공식/현장 확인 가능한 지점만 확정 존처럼 표현한다.
- 정확한 좌표가 없는 공식 안내는 `정확한 핀 현장 확인 필요` 등으로 표시한다.
- 업체 실시간 대수는 공식 API/제휴가 없는 한 표시하지 않는다.

## FR-010 2페이지 셸

- 좌측 1페이지는 `CamBus`다.
- 우측 2페이지는 `Picks`이다.
- 상단 탭으로 페이지를 전환한다.
- 모바일 수평 스와이프를 지원한다.
- 지도 패닝과 스와이프가 충돌하지 않도록 지도 제스처를 우선한다.
- 메인 지도 우측에는 Picks 이동 핸들을 제공한다.
- 페이지 전환 시 지도/경로/Guidance 상태를 보존한다.

## FR-011 Picks 세로형 피드

- 아이콘 4열 그리드를 사용하지 않는다.
- 콘텐츠를 **한 줄 전체 폭의 세로형 배너/카드**로 표시한다.
- 세로 스크롤로 탐색한다.
- 지원 유형 예:
  - `news`: 학교 공지/영대 소식
  - `utility`: 강의포털, 학사일정 등 유용한 링크
  - `food`: 맛집/카페 추천
  - 기타 운영자가 정의하는 유형
- 클릭 시 외부 URL을 새 탭에서 연다.
- 데이터는 `data/portal-feed.json`에서 읽는다.
- `enabled=false`는 숨긴다.
- `order` 오름차순으로 정렬한다.
- 비 HTTP(S) URL은 노출하지 않는다.

## FR-012 Picks 콘텐츠 데이터 모델

각 항목은 다음 필드를 지원한다.

```text
id
 type
 badge
 icon
 title
 summary
 url
 source
 publishedAt
 order
 enabled
```

`title`, `url`은 필수다.

운영자는 별도 관리자 웹페이지 없이 JSON 파일을 직접 수정한다.

## FR-013 Picks 인라인 광고

- **콘텐츠 5개마다 광고 1개**를 자동 삽입한다.
- 광고는 콘텐츠 JSON에 직접 넣지 않는다.
- 광고 삽입 위치는 `portal.js`가 계산한다.
- 광고에는 `광고` 라벨을 표시한다.
- Ezoic placement가 없으면 하우스 광고 또는 placeholder를 표시할 수 있다.
- 기본 하우스 광고 문의 주소는 `ad@uxo.kr`이다.
- 지역 맛집, 카페, 행사, 동아리, 지역 서비스 등의 광고 모집 문구를 표시할 수 있다.

예:

```text
콘텐츠 1~5
광고 1
콘텐츠 6~10
광고 2
콘텐츠 11~15
광고 3
```

## FR-014 사용자 수

Picks 최하단에 다음을 표시한다.

- 오늘 사용자 수
- 이번달 사용자 수

정의:

- `Asia/Seoul` 기준
- 익명 브라우저 토큰 기준 고유 사용자 추정치
- 원본 토큰은 서버에 저장하지 않고 SHA-256 해시만 저장
- 브라우저/기기 변경은 별도 사용자처럼 집계될 수 있음

## FR-015 Picks 최하단 광고

- 사용자 통계 바로 아래 별도 광고 배너를 둔다.
- 메인 CamBus 하단 광고와 별도 placement 사용을 지원한다.
- 기본 fallback으로 `영남대 · 경산 지역 광고 모집중 / ad@uxo.kr` 배너를 사용할 수 있다.

## FR-016 PWA

- Web App Manifest를 제공한다.
- Service Worker를 제공한다.
- 주요 정적 파일과 `portal-feed.json`을 캐시 가능하게 한다.
- `/api/*` 실시간 응답은 Service Worker 캐시에 저장하지 않는다.

---

# 6. 광고 요구사항

## ADV-001 최초 진입 광고

- 브라우저 세션 최초 1회 큰 광고를 표시할 수 있다.
- 즉시 닫기 가능해야 한다.
- 세션 내 반복 노출하지 않는다.

## ADV-002 CamBus 메인 하단 광고

- 화면 하단 얇은 배너를 유지한다.
- Guidance 중에도 유지한다.
- 안내 바, 지도 버튼, CTA와 겹치지 않도록 레이아웃 공간을 예약한다.

## ADV-003 Picks 인라인 광고

- FR-013의 5:1 규칙을 따른다.
- 광고 슬롯 수는 콘텐츠 개수에 따라 자동 증가한다.
- 네트워크 광고가 없는 슬롯은 하우스 광고로 대체 가능하다.

## ADV-004 Picks 최하단 광고

- 사용자 수 바로 아래에 표시한다.
- 별도 placement를 지원한다.

## ADV-005 Ezoic 어댑터

설정값:

```text
provider
launchPlacementId
linksPlacementId
feedPlacementIds[]
useEzoicAnchor
bannerHeight
```

실제 Ezoic 계정/승인/스크립트가 없는 개발 환경에서는 광고 네트워크 요청을 임의로 만들지 않는다.

## ADV-006 광고 UX 금지사항

- 지도 핵심 버튼을 광고가 가리면 안 된다.
- 승하차 경고를 광고가 덮으면 안 된다.
- 광고를 지도 마커/시스템 버튼처럼 위장하면 안 된다.
- 닫기 어렵게 설계하지 않는다.

---

# 7. 데이터 모델

## 7.1 Stop

```text
id
routeId
sequence
name
buildingCode
aliases[]
lat
lng
note
dwellSeconds
```

## 7.2 PortalFeedItem

```text
id: string
type: string
badge: string
icon: string
title: string
summary: string
url: http(s) URL
source: string
publishedAt: YYYY-MM-DD optional
order: number
enabled: boolean
```

## 7.3 RideTelemetry

```text
riderToken
routeId
tripKey
lat
lng
accuracy
clientTimestamp
serverTimestamp
expiresAt
```

## 7.4 CrowdingReport

```text
reportToken
routeId
tripKey
level
createdAt
expiresAt
```

## 7.5 VisitorStats

```text
days: {
  YYYY-MM-DD: [hashedVisitorToken...]
}
months: {
  YYYY-MM: [hashedVisitorToken...]
}
```

---

# 8. API 요구사항

## GET `/api/health`

서비스 상태 반환.

## GET `/api/live-buses`

TTL 내 크라우드 위치를 운행편별 집계하여 반환.

## POST `/api/telemetry`

동의한 탑승자 위치 샘플 수신.

## GET `/api/crowding`

현재 유효한 운행편별 혼잡도 집계 반환.

## POST `/api/crowding`

사용자 혼잡도 신고 수신.

## GET `/api/portal-feed`

`data/portal-feed.json`을 읽어 유효한 Picks 콘텐츠를 정렬하여 반환한다.

서버는 요청 시 파일을 읽으므로 운영자가 JSON을 변경한 뒤 서버 재시작 없이 반영할 수 있어야 한다.

## POST `/api/visit`

익명 브라우저 방문을 등록한다.

## GET `/api/visitors`

오늘/이번달 고유 브라우저 추정치를 반환한다.

---

# 9. 비기능 요구사항

## NFR-001 모바일 우선

- 360px 수준의 모바일 화면에서 핵심 기능이 사용 가능해야 한다.
- 피드 카드는 한 손 스크롤에 적합해야 한다.

## NFR-002 성능

- 지도와 피드는 서로 독립적으로 로딩 가능해야 한다.
- Picks 콘텐츠 JSON 실패가 지도 기능을 중단시키면 안 된다.

## NFR-003 장애 격리

- 광고 네트워크 실패가 길찾기 기능을 중단시키면 안 된다.
- 크라우드 API 장애 시 시간표 ETA로 폴백해야 한다.
- `/api/portal-feed` 실패 시 정적 `data/portal-feed.json`으로 폴백한다.

## NFR-004 개인정보 최소화

- 실명/학번/전화번호를 위치 공유 데이터와 결합하지 않는다.
- 방문자 집계는 원본 토큰 대신 해시를 저장한다.

## NFR-005 데이터 신뢰성

- 공식 정보와 커뮤니티/지역 검색 정보를 구분한다.
- 오래된 PM 안내는 최신 정보처럼 단정하지 않는다.
- 식당/카페의 영업시간과 영업 여부는 변동 가능함을 전제로 한다.

## NFR-006 접근성

- 주요 버튼은 텍스트 또는 접근성 라벨을 가진다.
- 외부 링크 카드가 키보드 포커스를 받을 수 있어야 한다.
- 광고 영역을 일반 콘텐츠와 구분한다.

## NFR-007 보안

- 외부 URL은 HTTP/HTTPS만 허용한다.
- API JSON body 크기를 제한한다.
- 기본 rate limit을 적용한다.
- 운영 배포는 HTTPS를 사용한다.

---

# 10. 데이터 운영 정책

## 10.1 Picks

운영 파일:

```text
data/portal-feed.json
```

운영 절차:

1. 출처 확인
2. URL 접속 확인
3. 짧은 요약 작성
4. `order` 배정
5. `enabled=true`
6. JSON 문법 검사
7. 저장
8. Picks 새로고침으로 확인

광고는 JSON에 넣지 않는다. 5개당 1개 규칙은 코드가 자동 처리한다.

## 10.2 데이터 출처 문서화

공개 검색으로 확보한 데이터는 `DATA_SOURCES.md`에 다음을 남긴다.

- 항목명
- 출처기관
- 원문 URL
- 확인일
- 최신성/주의사항

---

# 11. 버전 및 배포 규칙

제품 버전은 Semantic Versioning을 사용한다.

```text
MAJOR.MINOR.PATCH
```

최초 릴리스:

```text
1.0.0
```

모든 전달 ZIP은 버전을 파일명에 포함한다.

```text
bus-at-yu-v1.0.0.zip
```

ZIP 배포 시 반드시 다음을 함께 갱신한다.

- `VERSION`
- `CHANGELOG.md`
- `README.md`의 현재 릴리스
- `SRS.md`의 제품 릴리스
- `sw.js` 캐시 이름

모든 문서 `.md` 파일은 ZIP 내부 프로젝트 루트에 포함한다.

---

# 12. 수용 기준

## AC-001 메인 지도

- 앱을 열면 영남대 캠퍼스와 버스 노선이 표시된다.

## AC-002 길찾기

- 현재 위치와 목적지가 있으면 도보 및 가능한 버스 결합 경로를 비교할 수 있다.

## AC-003 ETA

- 실제 GPS가 없더라도 시간표 기반 ETA를 표시한다.
- 실제 GPS가 아님을 명확하게 구분한다.

## AC-004 Guidance

- 버스 결합 경로에서 승하차 안내를 시작/종료할 수 있다.

## AC-005 Picks 피드

- 우측 페이지에서 4열 아이콘 그리드가 아니라 세로형 카드가 표시된다.
- 카드가 많으면 상하 스크롤할 수 있다.
- 외부 링크를 열 수 있다.

## AC-006 5:1 광고

20개 정상 콘텐츠가 있을 때 피드 내부에 4개의 광고 위치가 생성된다.

## AC-007 지역 광고 모집

- 네트워크 placement가 없는 상태에서 `ad@uxo.kr` 지역 광고 모집 배너가 노출될 수 있다.

## AC-008 사용자 수

- Picks 최하단에서 오늘/이번달 수치를 볼 수 있다.
- 그 바로 아래 별도 광고 배너가 있다.

## AC-009 파일 운영

- `data/portal-feed.json`을 수정하고 새로고침하면 Node 서버 재시작 없이 피드가 갱신된다.

## AC-010 배포 패키지

- ZIP 파일명에 버전이 있다.
- `README.md`, `SRS.md`, `CHANGELOG.md`, `DATA_SOURCES.md`가 ZIP에 포함된다.

---

# 13. 1.0.0 구현 상태

구현됨:

- CamBus / Picks 2페이지 셸
- OSM 지도 및 두 순환버스 노선
- 시간표 ETA
- 도보/버스 결합 경로
- 승하차 Guidance
- 익명 Crowd 위치 공유 구조
- 혼잡도 신고 구조
- PM 레이어
- 최초 대형 광고 슬롯
- 메인 하단 광고
- Picks 세로 배너 피드
- 콘텐츠 5개당 인라인 광고 1개
- `ad@uxo.kr` 지역 광고 모집 하우스 배너
- 오늘/이번달 사용자 수
- Picks 최하단 별도 광고
- 파일 기반 피드 운영
- PWA shell

추후 고도화:

- 현장 정류장 GPS 실측
- 실제 구간 운행시간 표본 기반 ETA 보정
- 광고 네트워크 실 placement
- PM 지정 존 현장 좌표 수집
- 운영 DB/로그/모니터링
